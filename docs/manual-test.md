# 第1工程 手動テスト手順

実際の画面共有、マイク音声、保存先選択は人の操作が必要です。会社PCへ開発ツールをインストールせず、公開URLからテストします。テストには会社情報や個人情報を含まない画面を使用してください。

公開URL：<https://dache-web.github.io/ai-skill-recorder/>

## Chromeでの確認

1. Windows版Google Chromeを開きます。
2. `https://dache-web.github.io/ai-skill-recorder/` を開き、アドレスがHTTPSであることを確認します。
3. 「録画開始」を押します。
4. 画面共有画面で「画面全体」を選び、「共有」を押します。
5. マイクの許可画面が表示されたら「許可」を押します。
6. 「録画中」、増加する経過時間、「マイク取得中」が表示されることを確認します。
7. 会社情報を含まない画面で操作しながら、「Chrome音声確認」と発声します。
8. 1〜5分以内に「録画停止」を押します。
9. 録画確認画面で再生し、映像と声を確認します。
10. 一時停止し、「最初から再生」を押して先頭から再生できることを確認します。
11. 「PCへ保存」を押します。保存場所選択画面が出た場合は会社指定のテスト用フォルダを選びます。ブラウザ設定により自動でDownloads等へ保存された場合も正常です。
12. `.webm`ファイルがあり、サイズが0バイトではないことを確認します。
13. 保存したWebMを開き、映像と音声を確認します。
14. 短い2回目の録画を始め、Chromeの共有バーから「共有を停止」を押します。アプリが自動で録画確認画面へ進むことを確認します。

## Edgeでの確認

1. Windows版Microsoft Edgeを開きます。
2. 上記Chrome手順の2〜14を同じ順番で行います。
3. 発声内容だけ「Edge音声確認」に変えます。

## 異常操作の確認

- 画面共有をキャンセルしても、エラー案内後に再度「録画開始」を押せる。
- マイクを拒否した場合、「マイクなし」と表示して画面だけを録画できる。
- 保存をキャンセルしても動画が画面に残り、もう一度保存できる。
- 停止ボタンを続けて押しても、エラーや重複した動画が発生しない。

## 外部通信の確認（合格必須）

1. ブラウザ右上のメニューから「その他のツール」→「デベロッパー ツール」を開きます。
2. 「Network」を選び、通信記録を消去します。
3. 録画開始、録画中、録画停止、再生、PCへ保存を順番に行います。
4. HTML、CSS、JavaScript、favicon等の表示用ファイル以外に送信がないことを確認します。
5. `video/webm`、Blob、音声、画面画像、録画ファイルの外部送信が1件でもあれば不合格です。
6. 「Console」も確認し、重大なエラーがないことを確認します。

## 結果記録

| 項目 | Chrome | Edge |
|---|---|---|
| 公開URL・HTTPS | 成功 | 成功 |
| ブラウザバージョン | 未確認 | 未確認 |
| 画面共有 | 成功 | 成功 |
| マイク録音 | 成功 | 成功 |
| 録画 | 成功 | 成功 |
| アプリ停止 | 成功 | 成功 |
| 共有バーから停止 | 成功 | 成功 |
| アプリ内再生・映像 | 成功 | 成功 |
| アプリ内再生・音声 | 成功 | 成功 |
| 最初から再生 | 成功 | 成功 |
| WebM保存 | 成功 | 成功 |
| 保存WebMの再生 | 成功 | 成功 |
| 保存WebMの映像 | 成功 | 成功 |
| 保存WebMの音声 | 成功 | 成功 |
| 録画データの外部送信なし | 成功 | 成功 |
| 重大なコンソールエラーなし | 成功 | 成功 |
| favicon 404なし | 成功 | 成功 |
| DevTools Issues「No Issues」 | 成功 | 成功 |
| 未解決エラーID | なし | なし |

## 2026-08-17 会社PC受け入れテスト記録

### Chrome

- 実施環境: 会社PC / Google Chrome（完全なバージョンは未確認）
- 公開URLへのアクセス、HTTPS、アプリ表示: 成功
- 録画開始、画面共有選択、画面共有、録画、停止: 成功
- マイク録音: 成功。録音後、人がアプリ内再生で音声を確認
- アプリ内再生、映像、音声、最初から再生: 成功
- Chrome側の「共有を停止」: 成功。アプリが停止を認識し、正常に録画を終了
- PCへのWebM保存、保存WebMの再生・映像・音声: 成功
- Console: favicon 404なし、赤い重大エラーなし、Issuesは「No Issues」
- 受け入れ判定: 合格

### Chrome Network確認

- Network履歴を消去して短時間の録画開始、録画停止後に確認
- 表示された通信: `blob:https://dache-web.github.io/...` 1件、転送量0 B
- 判定: 成功 — `blob:`はブラウザメモリ内の録画を再生する一時URLであり、外部サーバーへの送信ではない
- WebM、Blobデータ、音声、画面画像、録画ファイル、AI API、外部APIの外部送信: 確認されず
- このNetwork確認用録画は画面表示が「マイクなし」の状態で実施。これは前項のマイク録音成功とは別の試験であり、Chromeのマイク利用失敗とは判定しない

### Edge

- 実施環境: 会社PC / Microsoft Edge（完全なバージョンは未確認）
- 公開URLへのアクセス、HTTPS、アプリ表示: 成功
- 画面共有、録画、マイク録音、停止: 成功
- アプリ内再生、映像、音声、最初から再生: 成功
- PCへのWebM保存、保存WebMの再生・映像・音声: 成功
- Edge側の「共有を停止」: 成功。アプリが停止を認識し、正常に録画を終了
- Console: favicon 404なし、赤い重大エラーなし、Issuesは「No Issues」
- 受け入れ判定: 合格

### Edge Network確認

- DevTools Networkで録画から停止までを実機確認
- 表示された主な項目: `blob:https://dache-web.github.io/...`、転送量0 B
- 判定: 成功 — ブラウザメモリ内の録画を再生するBlob URLであり、外部サーバーへの送信ではない
- WebM、Blobデータ、音声、画面画像、録画ファイル、AI API、外部APIへの録画データ送信: 確認されず

## 現在の判定

**第1工程完成**

GitHub PagesのHTTPS公開と、会社PCのChrome・Edgeによる画面共有、録画、マイク録音、停止、再生、WebM保存、ブラウザ側共有停止、外部送信なし、Console重大エラーなしを確認しました。第1工程の受け入れ条件はすべて合格です。

favicon修正では`index.html`の参照と`public/favicon.svg`だけを変更し、録画関連コードは変更していません。

## STEP2-1 解析準備の手動テスト

STEP2-1は実装検証中です。第1工程の完成checkpointは`step1-complete`タグで保持されています。

ChromeとEdgeの両方で、会社情報・個人情報を含まない30秒～2分の動画を使って確認します。

1. 画面とマイクを録画し、録画停止後に元WebMを正常に再生・保存できることを確認します。
2. 録画中にはポイント・動画区間・不要区間の操作を要求されないことを確認します。
3. 「02 マニュアルに使う場面を指定」で動画を再生し、「★ この場面をポイント追加」を複数回押します。
4. ポイント追加後も動画が停止せず、先頭へ戻らず、再生位置が勝手に変わらないことを確認します。
5. ポイント時刻とPNGの内容が一致し、PNGを保存できることを確認します。
6. ポイントの「確認」「現在位置へ変更」「削除」が動作し、変更後の時刻とPNGが更新されることを確認します。
7. ポイントが最大30件に制限され、超過時も元WebMと登録済みポイントが残ることを確認します。
8. 「動画区間 開始」後に動画を進め、「動画区間 終了」で開始・終了・秒数が記録されることを確認します。
9. 動画区間の「確認」「開始変更」「終了変更」「削除」を確認します。
10. 不要区間についても開始・終了・確認・開始変更・終了変更・削除を確認します。
11. 動画区間と不要区間を重複させ、警告が表示されても両方の指定が保持されることを確認します。
12. 終了位置が開始位置以前の場合に区間が追加されないことを確認します。
13. 補助画像の間隔を2秒・5秒・10秒へ変更し、一定間隔PNGを生成・保存できることを確認します。
14. 補助PNGが最大30枚を超えず、ポイントPNGと別セクションで表示されることを確認します。
15. JSONをコピー・保存し、`schemaVersion`が`step2-1-preview-2`であることを確認します。
16. JSONに`reviewAnnotations.points`、`videoSegments`、`excludedSegments`があり、画面の指定と一致することを確認します。
17. JSONの`originalWebM.immutableSource`が`true`、`automaticUpload`が`false`であることを確認します。
18. 保存済みWebMを読み込み、同じポイント・区間指定・解析データ生成ができることを確認します。
19. 保存済みWebMでは`hasAudio`が`unknown`となり、`limitations`に理由があることを確認します。
20. 操作後に元WebMを再保存・再生し、映像・音声・ファイルサイズに異常がないことを確認します。
21. NetworkでWebM、音声、JSON、PNGの外部自動送信がないことを確認します。
22. Consoleに重大エラーがないことを確認します。

### STEP2-1 再生準備・PNG修正の重点確認

1. 保存済みWebMを読み込み、動画を最後まで再生せず5秒付近まで再生します。
2. 「★ この場面をポイント追加」が成功し、押下直後に動画が同じ位置で一時停止することを確認します。
3. ポイントPNGが壊れた画像や黒一色ではなく、5秒付近の実画面であることを確認します。
4. ポイントPNGを保存してPCで開き、画像サイズと内容が正常であることを確認します。
5. 「▶ 再生を続ける」で同じ位置から再開できることを確認します。
6. 動画区間と不要区間は開始時に再生状態が変わらず、終了時に一時停止することを確認します。
7. AI解析用補助PNGについても画面表示と保存ファイルの内容を確認します。
8. Consoleに画像Blobに関するCSP違反がないことを確認します。
9. Networkで外部送信がなく、`connect-src 'none'`が維持されていることを確認します。
10. 全操作後も元WebMを保存・再生でき、映像・音声が正常であることを確認します。

### STEP2-1 Chrome実機テスト結果（2026-08-18）

- 実施環境: 会社PC / Google Chrome
- 保存済みWebMを最後まで再生せずポイント登録: 成功
- ポイント追加後の一時停止: 成功
- 「▶ 再生を続ける」: 成功
- 動画区間・不要区間の登録: 成功
- ポイントPNGの画面表示・PC保存: 成功
- AI解析用補助PNGの画面表示: 成功
- 残課題: 一覧確認中に動画と主操作が画面外へ消れ、上下スクロールが必要

### STEP2-1 確認操作改善の重点確認

1. ポイント・動画区間・不要区間を複数登録し、一覧を下へスクロールします。
2. PC幅では小型動画と主操作がsticky表示され、一覧を過度に隠さないことを確認します。
3. 狭い幅では縦表示へ戻り、横スクロールや極端な縮小がないことを確認します。
4. ポイントの「確認」で番号・時刻・拡大PNGを含むダイアログが開くことを確認します。
5. 「閉じる」とEscの両方でダイアログを閉じられることを確認します。
6. 動画区間の確認再生が開始位置から始まり、終了位置で停止することを確認します。
7. 不要区間も同様に確認でき、確認だけで指定内容が変わらないことを確認します。
8. 各ダイアログを閉じてもメイン動画の現在位置が変わらないことを確認します。
9. 既存のポイントPNG・補助PNG・元WebM保存を再確認します。
10. Console重大エラーなし、Network外部送信なしを確認します。

### STEP2-1 小型sticky UIのChrome評価（2026-08-18）

- 小型sticky動画と操作ボタンの表示維持: 技術的には成功
- stickyサイズと一覧の見やすさ: 不合格
- 不合格理由: 動画が小さく、録画画面内の文字・クリック対象・画面遷移を確認しながらポイントや区間を判断する用途に適さない
- ポイント拡大確認ダイアログ: 成功
- Escでダイアログを閉じる操作: 成功
- 動画区間の確認再生と終了位置停止: 成功
- 不要区間の確認再生: 成功
- 判定: sticky技術ではなく左右2列のUI設計を不合格とし、合格済みダイアログ機能を維持して上下分割へ再設計する

### STEP2-1 上下分割作業画面の重点確認

1. 「マニュアルに使う場面を指定」「マニュアルを作成する動画」「登録した内容」と表示されることを確認します。
2. ファイル選択がコンパクトに表示され、その直下に全幅の動画が表示されることを確認します。
3. 動画内の文字・ボタン・メニュー・画面遷移を判断できる大きさであることを確認します。
4. 通常のPC高さでは動画と主操作を維持したまま、登録結果領域だけをスクロールできることを確認します。
5. 高さが低い画面では通常の縦スクロールへ戻り、動画が極端に縮小されないことを確認します。
6. ポイント一覧は最大2列で、サムネイルだけでも画面内容を判断できることを確認します。
7. ポイント確認ダイアログ、Esc、区間確認再生、終了位置停止、メイン動画位置維持を再確認します。
8. ポイントPNG、補助PNG、JSON、元WebM、Console、Networkを回帰確認します。

### STEP2-1 区間終了ホワイトアウト修復の重点確認

- 静的調査上の最有力原因: 固定高の親Grid、入れ子Grid、動画の`height: 100%`、結果欄スクロール、操作欄・メッセージ欄の同時変化による過拘束な再レイアウト
- 注意: 会社PCで症状発生時のConsoleエラーを取得できていないため、原因確定とは記録しない

ChromeのConsoleを開いた状態で、次を順番に実施します。

1. 読み込み直後から「▶ 再生する」が操作欄の左端にあり、現在位置から再生できることを確認します。
2. 再生中は同じボタンが「⏸ 一時停止」に切り替わり、停止後は同じ位置で再生操作へ戻ることを確認します。
3. 動画区間を「開始→終了」5回連続で登録し、5件保持、ID重複なし、終了時一時停止、開始ボタン復帰を確認します。
4. 不要区間も「開始→終了」5回連続で登録し、同じ項目を確認します。
5. 動画区間→不要区間→動画区間→不要区間→動画区間の順で登録し、動画区間3件・不要区間2件になることを確認します。
6. ポイント10件・動画区間5件・不要区間5件の計20件を登録し、画面、動画、一覧、確認ダイアログが維持されることを確認します。
7. 指定中は種類と開始時刻、完了後は開始・終了時刻が操作欄下の固定状態欄へ表示されることを確認します。
8. 登録件数やメッセージによって大画面動画の寸法が変動せず、通常PC高さでは登録結果だけをスクロールできることを確認します。
9. 低い画面ではページ全体の縦スクロールへ戻ることを確認します。
10. 全操作でホワイトアウト、React画面消失、Console重大エラーが一度も発生しないことを確認します。

ホワイトアウトが再発した場合はその場で試験を停止し、Consoleエラー全文、発生操作、何回目か、動画時刻、ポイント・動画区間・不要区間の登録件数を記録します。再試行で上書きしません。

### STEP2-1 固定操作バー・上下分割UIの重点確認

1. 動画下の操作が「再生」「★ポイント」「動画区間」「不要区間」「キャンセル」の5個・同じ順序・同じ寸法で常時表示されることを確認します。
2. 文字が「開始」「終了」「再開」へ変わっても、5ボタンの横幅・高さ・位置が動かないことを確認します。
3. ポイント追加直後に同じポイントボタンを押すと、ポイントを重複登録せず同じ位置から再生することを確認します。
4. 標準プレイヤーまたは左端ボタンで手動停止した場合、ポイントボタンが新しいポイントを登録することを確認します。
5. 動画区間を「開始→終了・停止→同じボタンで再生」の順で操作し、次の押下から新しい指定を開始できることを確認します。
6. 不要区間も同じ3段階で操作できることを確認します。
7. キャンセルは進行中の区間だけを解除し、登録済みポイント・区間・元WebMを変更しないことを確認します。
8. 分割バーをマウスで上下へ動かし、上側と下側の高さを変更できることを確認します。
9. ドラッグ中もvideo要素が再読み込みされず、currentTime、再生状態、指定中状態、登録内容が維持されることを確認します。
10. 分割バーを離した後に再読み込みし、前回の比率が復元されることを確認します。localStorageには`ai-skill-recorder.reviewSplitRatio.v1`の数値だけがあることを確認します。
11. ウィンドウを最大化・縮小し、実際のレビュー領域寸法に合わせて上下最低高が維持されることを確認します。
12. 低い画面では通常の縦スクロールへ戻り、大画面動画が操作不能な大きさへ縮まらないことを確認します。
13. ポイント・動画区間・不要区間の「上で確認」でページが勝手にスクロールせず、上側メイン動画の時刻だけが対象位置へ移動することを確認します。
14. 「画像を拡大」「区間だけ再生」の補助ダイアログ、Esc、区間終了位置停止、メイン動画位置維持を確認します。
15. 動画区間と不要区間を各5回、混在5件、ポイント10件＋各区間5件の計20件で再試験し、途中でも分割バーを動かします。
16. Console重大エラー、ホワイトアウト、動画時刻リセット、登録消失がなく、Network外部送信なし、`connect-src 'none'`維持を確認します。

解析検証の基本セットは、元WebM、`analysis.json`、`point_XXX.png`、必要に応じた`frame_XXXX.png`です。取得したセットを人がChatGPTへ明示的に渡し、AIによる編集範囲の判断を検証するまで、STEP2-1完成とは判定しません。
